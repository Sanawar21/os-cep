# Testing
To redo a test, copy the `CONFIG` in the test file and paste it in `config.py`.
Then run `python main-no-ui.py`

## Inconvenience
The tests have to be done in this way because the project's file structure wasn't suitable for a more modular testing configuration. We apologize for the inconvenience caused.  